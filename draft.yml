models:
  tb_companies:
    cmp_name: string
    cmp_img: string
    cmp_latitude: string
    cmp_longitude: string
    cmp_address: string
    cmp_url: string
    cmp_instagram: string
    fds_facebook: string
    fds_state: boolean
    cmp_id_usr: id:tb_users
    cmp_id_dst: id
    cmp_id_cmpt: id
    relationships:
      hasOne: tb_district, tb_company_types
  tb_company_types:
    cmpt_desc: longtext
